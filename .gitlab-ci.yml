image: node

before_script:
  - npm i -g firebase-tools

deploy:
  stage: deploy
  script:
    - echo "installing node modules"
    - yarn install
    - echo "install done"
    - echo "firebase login"
    - firebase login:ci
    - echo "deploy starting"
    - firebase deploy
