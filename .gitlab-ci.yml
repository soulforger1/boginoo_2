image: node

before_script:
  - npm i -g firebase-tools

deploy:
  stage: deploy
  script:
    - echo "installing node modules"
    - yarn install
    - echo "install done"
    - echo "deploy starting"
    - firebase deploy
